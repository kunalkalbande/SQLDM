namespace Idera.SQLdm.Common.Import {
    /// <summary>
    /// These values correspond to the radio buttons on the ODBC tag
    /// of the 4.x Configure Alert Destinations dialog.
    /// </summary>
    public enum ODBCOption {
        MachineDataSource,
        FileDataSource,
        Other
    }
}
